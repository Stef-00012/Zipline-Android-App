import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	checkbox: {
		borderRadius: 6,
		width: 20,
		height: 20,
		borderWidth: 2,
		justifyContent: "center",
		alignItems: "center",
	},
	checkboxContainer: {
		marginRight: 10,
	},
});
