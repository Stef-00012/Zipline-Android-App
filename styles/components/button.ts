import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	button: {
		borderRadius: 6,
		height: 40,
		alignItems: "center",
		flexDirection: "row",
	},
	buttonText: {
		textAlign: "center",
	},
});
